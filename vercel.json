{
  "version": 2,
  "buildCommand": "cd api && npm install --production",
  "outputDirectory": "api",
  "rewrites": [
    { "source": "/(.*)", "destination": "src/server.js" }
  ],
  "env": {
    "WEB_ROOT": "../public",
    "NODE_ENV": "production"
  }
}
